[Unit]
Description=Kibana5 ElasticSearch GUI - Instance %I
Requires=network.target
After=network.target

[Service]
Type=simple
User=root
ExecStart=/opt/kibana5/bin/kibana -c /opt/kibana5/config/kibana.%i.yml

[Install]
WantedBy=multi-user.target
